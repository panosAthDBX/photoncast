Summary:
Status: Needs Work

Required Actions:
- Code owner (repo hygiene) — Fix repo-wide formatting drift before PR; `cargo fmt --check` is still failing with many unrelated diffs outside the feature scope.
- Feature owner (search/backend) — Clarify and/or finish backend consolidation scope (Task Group 6.3 is partial: provider + launcher helper migrated, but broader direct Spotlight/walkdir call paths still exist).
- PR author — Split or isolate unrelated changes/spec artifacts if they’re not intended for this PR (`droidz/specs/...` untracked content and broad existing modified set increase review risk).
- Reviewer + author — Ensure branch strategy is corrected (current branch is `main` with a large dirty working tree; create/clean feature branch before opening PR).

Tests:
- Verified pass: `cargo check --workspace`
- Verified pass: `cargo clippy --workspace -- -D warnings`
- Verified pass: `cargo test --workspace`
- Verified pass: targeted suites in touched areas (`search::engine`, `search::providers::files`, `search::file_search_backend`, `search::spotlight::prefetch`, `indexer::watcher`)
- Verified pass: `cargo bench -p photoncast-core --no-run`
- Verified pass: `cargo build -p photoncast-core --features perf`
- Blocking gate: `cargo fmt --check` fails (unrelated formatting diffs present)

Documentation & Notes:
- Changelog updates: None observed/required yet, but PR description should explicitly call out deferred items (especially partial Group 6 migration).
- Added optional dependencies/features (`tracing-flame`, optional `tracing-subscriber`, `perf` feature) should be documented in PR notes for reviewers.
- TODO/FIXME scan in touched core files: none found.
